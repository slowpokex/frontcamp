import 'babel-polyfill';
import assert from 'assert';
import { FluxContainer } from '../src/app/services/scope';

describe('FluxContainer', function() {
    it('should return empty', () => {      
        assert.equal(0, 0);
      });
});